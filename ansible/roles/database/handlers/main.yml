---
- name: Restart PostgreSQL
  systemd:
    name: "postgresql@{{ postgres_version }}-main"
    state: restarted

- name: Reload PostgreSQL
  systemd:
    name: "postgresql@{{ postgres_version }}-main"
    state: reloaded
